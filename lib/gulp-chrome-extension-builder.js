'use strict';

var Stream = require('stream');
var Path = require('path');

function ceb(obj) {

   /// var stream = new Stream.Transform({objectMode: true});

    console.log(1);
    return stream;
}

module.exports = ceb;
